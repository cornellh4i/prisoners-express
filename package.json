{
	"name": "pe-app",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "cd api && npm install --legacy-peer-deps && npm start",
		"heroku-postbuild": "cd client && npm install --legacy-peer-deps && npm run build"
	},
	"engines": {
		"node": "16.12.0"
	},
	"keywords": [],
	"author": "",
	"license": "ISC"
}
